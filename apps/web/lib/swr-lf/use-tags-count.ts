/**
 * License-free tags count hook
 * Re-export of existing useTagsCount() for convenience in LF code
 * Used for tags count in usage breakdown cards
 * The tags endpoint is unchanged and license-free
 */
export { default } from "../swr/use-tags-count";
