/**
 * License-free users hook
 * Re-export of existing useUsers() for convenience in LF code
 * Used for teammates count in usage breakdown cards
 * The users endpoint is unchanged and license-free
 */
export { default } from "../swr/use-users";
